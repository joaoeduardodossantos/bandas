<%= form_with(model: estilo, local: true) do |form| %>
  <% if estilo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(estilo.errors.count, "error") %> prohibited this estilo from being saved:</h2>

      <ul>
        <% estilo.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
